# 定期発注連絡自動作成の実行計画書

## 1. 可能性の結論

はい、可能です。現在の `memo-desk` は主にブラウザ上のメモ管理アプリですが、定期発注連絡の自動生成・下書き作成を実現するには、以下のどちらかの外部機能・バックエンド機能を追加する必要があります。

- Firebase Cloud Functions / Cloud Scheduler を使う方法
- Google Apps Script / Power Automate / Zapier などの外部自動化サービスを使う方法

## 2. 前提条件

1. 発注内容とスケジュール情報を管理するデータ構造が必要
   - 発注先・品目・数量・納期・担当者など
2. メール文テンプレートが必要
   - 定期発注連絡の本文、件名、差し込み項目
3. 自動生成した内容を「下書き状態」で送るための送信インターフェース
   - Gmail API でドラフト作成
   - または外部メールサービスのドラフト機能
4. 発注担当者のメールアドレスと通知先が確定していること

## 3. 追加開発の大まかな方針

### A. アプリ構成

1. `発注予定` を管理する機能を用意する
   - 既存メモを拡張するか、新しいデータコレクションを追加
2. 定期実行のスケジュール管理
   - 週次、月次、任意日などを指定可能にする
3. メールテンプレート設定画面
   - 件名、本文、担当者、CC/BCC などを入力できるようにする
4. 自動生成・下書き作成処理
   - 定期処理が起動したときにメール本文を組み立てる
   - 担当者に「下書きとして送る／送信依頼する」仕組み

### B. 技術選択例

- フロントエンド: 既存の `memo-desk` に追加画面を組み込む
- バックエンド:
  - Firebase Cloud Functions + Cloud Scheduler
  - Gmail API を使ってドラフトを作成
- データ保存: Firestore もしくはブラウザローカル（本番なら Firestore 推奨）

## 4. 工程（短期実装ステップ）

### Step 1: 要件整理

- 発注対象と周期パターンを定義
- 送信先と担当者連絡ルールを確定
- どの時点で「下書き」とするか決定

### Step 2: データモデル設計

- `orders` collection 例:
  - `itemName` 発注品目
  - `quantity` 数量
  - `frequency` 毎週/毎月/指定日
  - `nextRunDate` 次回実行日
  - `recipientEmail` 担当者メール
  - `templateId` テンプレート参照

- `templates` collection 例:
  - `subject` 件名
  - `body` 本文テンプレート
  - `variables` プレースホルダー

### Step 3: UI追加

- 発注予定登録画面
- 定期実行設定画面
- テンプレート管理画面
- 下書き生成・確認画面

### Step 4: 自動化ロジック実装

- 定期実行トリガー
  - Firebase Cloud Scheduler で cron 実行
- ドラフト生成
  - 対象データを取得
  - テンプレートに差し込み
  - 担当者宛にドラフトを作成

### Step 5: テストと運用確認

- 正しい周期で実行されるか
- ドラフト本文が正しいか
- 送信先・CC が想定通りか
- 既存アプリに影響がないか

## 5. 実行計画の概要（工程表）

1. 企画・要件定義（1日〜2日）
2. データ設計・UI設計（1日）
3. フロントエンド実装（2〜3日）
4. バックエンド実装（2〜3日）
5. 結合テスト・調整（1〜2日）
6. リリース準備・操作手順書作成（1日）

> 合計目安: 1〜2 週間程度（要件の確定と既存環境の整備状況による）

## 6. 重要な注意点

- 現状の `memo-desk` はブラウザ単体のメモ管理アプリです。メール自動化を行うには、サーバー側での定期実行または外部サービスの接続が必要です。
- Gmail API を使う場合は、Google OAuth による認可と API 利用設定が必要です。
- 送信先担当者の承認フローやメール下書きの確認方法を明確に設計する必要があります。

## 7. 次のアクション

1. どの範囲まで自動化したいか決定してください。
   - 例: 「ドラフトを作るまで」「担当者に通知するまで」「自動送信するまで」
2. 使用予定のメールサービスを決めてください。
   - 例: Gmail、Outlook、社内メールサーバー
3. 既存 `memo-desk` に組み込むか、別システムとして作るかを判断してください。

---

この文書は `memo-desk` プロジェクトのルートに `EMAIL_ORDER_AUTOMATION_PLAN.md` として保存しました。